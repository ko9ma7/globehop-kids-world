# GlobeHop V8

> 최신 버전: V8.5 — 33,801개 도시 검색, 국가별 도시 디렉터리, 도시별 구조화 정보·날씨·대기질·주변 지식 장소 탐험이 추가되었습니다.

## V8에서 먼저 확인할 기능

1. 나라를 선택하면 `도시 탐험` 영역에 주요 도시 목록이 표시됩니다.
2. 도시를 검색하거나 도시 목록을 누르면 도시 고유 상세 정보가 표시됩니다.
3. 온라인 연결 시 Open-Meteo 날씨/대기질, Wikipedia 도시 설명/주변 장소, Wikidata 구조화 정보가 보강됩니다.
4. 자세한 데이터 확장 방법은 `docs/CITY_DATA_EXPANSION.md`를 참고하세요.

# 기존 지도/지구본 수정 기록

이번 V5는 사용자가 제공한 실제 브라우저 화면에서 확인된 **2D/3D 지도 시각화 이상**을 바로잡는 버전입니다.

## 가장 중요한 수정

### 1. 2D 지도

이전 버전은 지도를 확대할 때 SVG 내부의 비행기, 출발/도착 핀, 도시 점까지 함께 확대되어 화면을 덮었습니다.

V5에서는 지도 자체만 확대하고 마커는 역스케일을 적용해 **화면에서 거의 일정한 크기**를 유지합니다.

서울 → 일본 기본 예시에서:

- 한국과 일본의 실제 국가 윤곽을 확대 범위 계산에 포함
- 한국과 일본이 함께 화면 안에 들어옴
- 과도한 7배 이상 확대 방지
- 비행기/핀은 정상 크기 유지

### 2. 3D 지구본

짧은 지역 이동에서 일본 전체 열도 외곽까지 모두 맞추려다 카메라가 멀어지는 문제를 제거했습니다.

서울 → 도쿄 같은 짧은 구간은 **지역 확대 카메라**를 사용합니다.

- 출발지와 목적지가 충분히 떨어져 보임
- 대한민국/일본 국가 강조 유지
- 국가명은 지구 표면에도 표시
- 중복되는 국가/도시 라벨 제거
- 선택 국가의 점은 최대 개수를 제한해 복잡도를 낮춤

### 3. 캐시

`public/sw.js`의 캐시 키가 V5로 변경되었습니다. GitHub Pages에 새 `dist`를 덮어쓰면 새 코드가 우선 로드됩니다.

## 실행

```bash
npm install
npm run dev
```

2D 강제 테스트:

```text
http://localhost:5173/?c=JP&view=map
```

3D 강제 테스트:

```text
http://localhost:5173/?c=JP&view=globe
```

## GitHub Pages에 올릴 때

개발 프로젝트 전체를 Actions로 배포하려면 프로젝트 전체를 저장소에 올립니다.

정적 파일만 직접 올리는 방식이라면 **`GlobeHop_3D_V5_GITHUB_PAGES_DIST.zip`의 내용**을 저장소 루트에 덮어쓰세요.

페이지의 빌드 배지에 **V8.5**이 표시되는지 확인하면 새 버전 적용 여부를 바로 알 수 있습니다.
